# 🚀 AirQuality Data Platform

Ce projet met en place une **plateforme de traitement et d’analyse de données qualité de l’air** à l’aide de **Docker Compose**.  
Il inclut plusieurs services pour le stockage, l’orchestration, l’analyse et la visualisation des données.

---

## 📦 Services inclus

- **MinIO** : Stockage S3 local pour les fichiers et datasets.  
- **PostgreSQL** : Data Warehouse pour stocker les métriques qualité de l’air.  
- **Airflow** : Orchestration des pipelines de données.  
- **PySpark (Jupyter Notebook)** : Analyse et transformation des données distribuées.  
- **Grafana** : Visualisation des données qualité de l’air.  

---

## ▶️ Démarrage

### Lancer les conteneurs
```bash
    docker-compose up -d

## Initialisation
    1. Initialiser la base Airflow
    docker-compose run --rm airflow-webserver airflow db init

## Créer la base et la table dans PostgreSQL

    docker exec -it airquali-track-postgres-1 psql -U admin -d airquality

## Exécuter le script SQL :
    CREATE TABLE IF NOT EXISTS air_quality_metrics (
        id SERIAL PRIMARY KEY,
        location_name VARCHAR(255),
        city VARCHAR(255),
        country VARCHAR(255),
        parameter VARCHAR(50),  -- ex: pm25, pm10, so2
        value DOUBLE PRECISION,
        unit VARCHAR(20),
        last_updated_utc TIMESTAMP,
        lat DOUBLE PRECISION,
        lon DOUBLE PRECISION,
        loaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    -- Index pour accélérer les requêtes
    CREATE INDEX IF NOT EXISTS idx_parameter ON air_quality_metrics(parameter);
    CREATE INDEX IF NOT EXISTS idx_location ON air_quality_metrics(location_name);
    CREATE INDEX IF NOT EXISTS idx_country ON air_quality_metrics(country);

## pour installer le paquet cryptography
    pip install cryptography

## Générez une Fernet Key "Fernet KEY "
    python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

## Générez une Secret Key
    python -c "import secrets; print(secrets.token_hex(32))"

## Debug et logs
    docker-compose logs -f airflow-webserver
    Vérifier un autre service

## Pour céer les identifiants et se connecter la plateforme airflow
    docker-compose run --rm airflow-webserver airflow users create `
    --username admin `
    --firstname Admin `
    --lastname User `
    --role Admin `
    --email admin@example.com `
    --password admin
```

## pour ajouter une clé pour se connecter et importer les données.
    airflow variables set OPENAQ_API_KEY "ta_cle_api"

Interfaces disponibles

    MinIO Console : http://localhost:9001
    Identifiant : minioadmin / Mot de passe : minioadmin

    PostgreSQL : localhost:5432 (user: admin, password: password)

    Airflow Webserver : http://localhost:5050

    Jupyter PySpark Notebook : http://localhost:8888
    (token : airquality)

    Grafana : http://localhost:3000
    (user: admin, password: admin)
